export const useUser = async () => {
  // const  [getUser, {isLoading} ] = useGetUserQuery();
  // // if (isLoading) return <div>Loading...</div>;
  // // if (error) return <div>Error: {error.message}</div>;
  //
  // return await getUser().unwrap();
  return null;
};
