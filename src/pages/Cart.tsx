import styled, { css } from "styled-components";
import CartForm from "../features/auth/components/Cart/CartForm.tsx";

function Cart() {
  return (
            <CartForm/>
  );
}
export default Cart;
