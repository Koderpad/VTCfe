import styled, { css } from "styled-components";
// import CartForm from "../features/auth/components/Cart/auth/components/CartForm.tsx";
import CartForm from "../features/common/cart/components/CartForm";

function Cart() {
  return (
    <div>
      <CartForm />
    </div>
  );
}

export default Cart;
